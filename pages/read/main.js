export default function read(n){
    const read = document.createElement("div")
    read.innerHTML = `openning read ${n}`
    //article.appendChild(top())
    //article.appendChild(body())
    //article.appendChild(bottom())
    return(read)
}